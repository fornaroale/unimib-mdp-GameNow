<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.unimib.disco.gruppoade.gamenow">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".handlers.FirebaseHandler"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.FullScreenActivity"
            android:theme="@style/Theme.Design.NoActionBar"
            android:configChanges="orientation|layoutDirection|screenSize|screenLayout" />
        <activity
            android:name=".activities.UserInitializationActivity"
            android:configChanges="orientation|layoutDirection|screenSize|screenLayout"
            />
        <activity
            android:name=".activities.MainActivity"
            android:configChanges="orientation|layoutDirection|screenSize|screenLayout"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>